#include <stdio.h>
#include <string.h>
int main(){
    char name[] = "Rohan";
    
   int count = 0;
   do{
    if(count != strlen(name)){
       
        count++;
    }
    else if(count == strlen(name){
        printf("%s",name);
    }
    else{
    printf("gggS")
   }
   }
   
    while(count < strlen(name));
}
